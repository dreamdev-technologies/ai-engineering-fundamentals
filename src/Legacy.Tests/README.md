# Legacy.Tests

Empty on purpose. Activity 07 fills this project with characterisation tests for `src/Legacy`.

The project is wired up (xUnit, coverlet for coverage) so the first test you add runs with `dotnet test`.
